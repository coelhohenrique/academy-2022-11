version: 2

models:
  - name: fct_sales
    description: Tabela de fato vendas
    columns:
      - name: id_order
        description: Foreing e primary key da dimensão produto
        tests:
          - unique
          - not_null
          